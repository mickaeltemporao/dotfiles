.First <- function(){
}

.Last <- function(){
}

options(prompt="> ")
options(continue="... ")
options(defaultPackages=c(getOption("defaultPackages"),
                          "tidyverse"))

q <- function (save="no", ...) {
  quit(save=save, ...)
}
